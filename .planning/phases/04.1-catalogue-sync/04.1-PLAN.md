---
phase: 04.1-catalogue-sync
plan: "01"
subsystem: data
inserted: true
status: executing
tags: [data, catalogue, content, seo, sea]
source: "Google Sheet « Affretix - Catalogue » (gid 1827952040)"
---

# Phase 04.1 (INSERTED): Réalignement du catalogue sur le Google Sheet

## Contexte

Le catalogue de référence est désormais le Google Sheet « Affretix - Catalogue ».
Colonne A = Famille (catégorie), Colonne B = Matériel + profondeur de gamme.
La page produit est le **point d'entrée SEA** principal (la home sera peu vue) :
priorité à la fiche produit, qui sera ensuite « fine-tunée » puis dupliquée.

## Décisions validées (utilisateur)

1. **Périmètre** : remplacement complet. On supprime les 8 familles existantes et on
   pousse exactement les 3 familles du Sheet.
2. **Orthographe** : corrigée à l'affichage (ferraille, pneus, caoutchouc, balayeuse, tri).
3. **Accessoires Terrassement** : traités comme produits séparés (8 entrées).
4. **Workflow** : méthode GSD respectée (plan → checkpoint → exécution → vérification).
   Note : les commandes `/gsd-*` ne sont pas installées dans l'environnement Cowork ;
   méthode appliquée manuellement.
5. **Libellés ambigus** (Bulldozer 3→8, Monocylindre 1→5) : chiffres bruts conservés,
   aucune unité inventée. Descriptions de familles/produits : factuelles courtes
   (placeholder acceptable, le fine-tuning portera sur une fiche).

## Structure cible

- `terrassement` — 23 produits (dont 8 accessoires)
- `transport-et-voirie` — 3 produits
- `compactage` — 3 produits
- Total : 29 produits, 142 variantes.

## Fichiers impactés

- `src/data/families.ts` — 8 → 3 familles
- `src/data/machines.ts` — remplacement complet
- `src/components/layout/Footer.tsx` — liens familles (slugs obsolètes)
- `src/components/home/Hero.tsx`, `Reassurance.tsx`, `src/app/layout.tsx` — mentions « 67 types »
- `src/components/catalogue/MachineCard.tsx` + page produit — clé React des variantes

## Vérification

- `tsc --noEmit` + `eslint` propres
- Contrôle programmatique : familySlug valides, routes/slugs uniques, comptage variantes
- Build Next (à confirmer)
